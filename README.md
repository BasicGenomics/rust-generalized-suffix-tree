# rust-generalized-suffix-tree
Implementation of Generalized Suffix Tree using Ukkonen's algorithm in Rust
This implementation uses Vec<u32> instead of String and is only suitable for checking the existence of substrings (no unique terminating values).
